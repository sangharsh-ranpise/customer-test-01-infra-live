repos:
  - id: /.*/
    apply_requirements: [approved, mergeable]
    allowed_overrides: [workflow]

workflows:
  terragrunt:
    plan:
      steps:
        - run: terragrunt init
        - run: terragrunt plan
    apply:
      steps:
        - run: terragrunt apply --auto-approve